api_gateway_sql:
  timeout: 10s
  auth:
    enabled: true
    username: test
    password: test@test
  databases:
  - name: school
    type: sqlite
    dbname: "/tmp/test"
  targets:
  - name: list-student
    data_source_name: school
    multi: false
    sql: "select * from student"